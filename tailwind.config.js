const { colors } = require("tailwindcss/defaultTheme");

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.blue["800"],
      },
    },
  },
  variants: {},
  plugins: [],
};
